package com.dbboys.model;

/** Leaf under scheduler jobs folder (name only). */
public class SchedulerJob extends TreeData {

    public SchedulerJob(String jobName) {
        super(jobName);
    }
}
