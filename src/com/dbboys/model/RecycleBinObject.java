package com.dbboys.model;

/** Leaf under Oracle recycle bin folder (display name only). */
public class RecycleBinObject extends TreeData {

    public RecycleBinObject(String objectName) {
        super(objectName);
    }
}
