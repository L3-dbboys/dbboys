### 聚合函数
|函数名|说明|
|---|---|
|avg(x)|返回x的平均值|
|count(x)|返回一个包含x的查询返回的行数|
|max(x)|返回x的最大值|
|min(x)|返回x的最小值|
|sum(x)|返回x的和|
|median(x)|返回x的中间值|
|stddev(x)|返回x的标准差|
|variance(x)|返回x的方差|

### 字符串处理函数
|函数名|说明|
|---|---|
|ascii|返回ascii码                   
|substr|截取字符串                
|instr|字符串查找                 
|concat/\|\||字符串拼接             
|length|字符串长度                
|lengthb|字符串长度byte           
|lower|字符串转小写               
|upper|字符串转大写               
|ltrim|字符串左边截取             
|rtrim|字符串右边截取            
|trim|字符串两边截取             
|replace|字符串替换              
|lpad|字符串左边填充             
|rpad|字符换右边填充             
|to_number|字符串转换为number类型
|to_date|字符串转换为日期类型  
### 数学函数
|函数名|说明|
|---|---|
abs(x)|返回x的绝对值
floor(x)|返回小于等于x的最大整数值
mod(x,y)|返回x除以y的余数
power(x,y)|返回x的y次幂
round|四舍五入
sign(x)|返回x的正负标志
sqrt(x)|返回x的平方根

### 日期和时间函数
|函数名|说明|
|---|---|
sysdate|获取服务器时间
current|获取服务器时间戳
current year to day|获取年到日
to_date|字符串转日期
add_months|月份加减
last_day|获取月份最后一天

### 类型转换函数
|函数名|说明|
|---|---|
::|
cast|
### guid函数
|函数名|说明|
|---|---|
sys_guid()|
### 加密函数
```
CREATE TABLE customer (id CHAR(20), creditcard CHAR(107));
SET ENCRYPTION PASSWORD 'abc123' WITH HINT 'zimushuzi';会话级别
INSERT INTO customer VALUES ('1001', ENCRYPT_AES('1234567890123456'));
SELECT id, DECRYPT_CHAR(creditcard,'abc123') FROM customer;
```
### 递归函数connect
```
```
### group by rollup
```
```