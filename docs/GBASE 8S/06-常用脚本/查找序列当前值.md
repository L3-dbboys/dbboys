序列默认cache 20，以下sql查到的值为下次cache开始值：
```sql
SELECT cur_serial8 FROM systables t,sysmaster:sysptnhdr p
WHERE t.partnum=p.partnum
AND t.tabname='seqname'
```
要能在系统表查询到序列当前值，需设置序列cache为1，如:
```sql
create sequence seq1 cache 1;
```
或
```sql
ALTER SEQUENCE seq cache 1;
```