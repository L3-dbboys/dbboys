### datastudio按以下操作步骤防止长时间未使用断连

1，驱动配置，在 数据库 –> 管理驱动 中，选上GBase8s，进入编辑
在 编辑驱动“GBase8s”中，配置 PING查询 中增加 SELECT 1 FROM DUAL 语句

2（可选），首选项配置，在 窗口 -> 首选项 -> 常规 中，点选上 保持下一个/上一个编辑器、视图和透视图处于打开状态
在 窗口 -> 首选项 -> 数据库 -> 元数据 中，点选上 打开独立于元数据读取的连接

3，数据库连接
新建GBase 8s连接。编辑连接属性，在常规中，填写 保活时间 60 （秒）

重新连接到数据库。