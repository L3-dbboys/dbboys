如果应用与数据库不在同一网段，如应用在【10.10.10.x】，数据库在【192.168.1.x】，可能出现连接极慢的情况（5秒以上），参考以下步骤处理：
应用服务器【root】用户执行：
```
sed -i "s#^hosts.*#hosts:      files#g" /etc/nsswitch.conf
```
数据库服务器【root】用户执行：
```
sed -i "s#^hosts.*#hosts:      files#g" /etc/nsswitch.conf
```
重启应用。