1、在sqlhosts中增加一个假的server名称,如：
```
echo "idspri onsotcp 192.168.1.1 9088">>$GBASEDBTSQLHOSTS
```
2、启动数据库到最后一个检查点
```
oninit -PHY
```
3、数据库设置为集群备机
```
onmode -d secondary idspri
```
4、切换数据库到标准模式
```
onmode -d standard
```
(Clearing the physical and logical logs has started)需要等待一段时间