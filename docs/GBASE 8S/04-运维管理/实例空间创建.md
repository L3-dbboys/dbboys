### 创建数据库空间语法
要创建数据库空间，使用onspace -c命令，语法如下：
```
onspaces  { -c { -d <DBspace> [-k <pagesize>] [-t]
                    -p <path> -o <offset> -s <size> [-m <path> <offset>] } |
               { -d <DBspace> [-k <pagesize>]
                    -p <path> -o <offset> -s <size> [-m <path> <offset>]
                    [-ef <first_extent_size>] [-en <next_extent_size>] } |
               { -P <PLOGspace>
                    -p <path> -o <offset> -s <size> [-m <path> <offset>] } |
               { -b <BLOBspace> -g <pagesize>
                    -p <path> -o <offset> -s <size> [-m <path> <offset>] } |
               { -S <SBLOBspace> [-t]
                    -p <path> -o <offset> -s <size> [-m <path> <offset>]
                    [-Mo <mdoffset>] [-Ms <mdsize>] [-Df <default-list>] } 
               { -x <Extspace> -l <Location> } }
```
**注意：在创建数据库空间之前，需要确认数据文件路径的设备存在，要求属主为gbasedbt:gbasedbt，且权限为660**
### 示例1-创建标准数据库空间
创建页大小为16k的标准数据库空间（空间名datadbds01，chunk路径/data/gbase/datachk01，偏移量为0，大小1024000KB，页大小16k）
```
touch /data/gbase/datadbs01chk001 
chmod 660 /data/gbase/datadbs01chk001 
onspaces -c -d datadbs01 -p /data/gbase/datadbs01chk001 -o 0 -s 1024000 -k 16
```
### 示例2-创建临时数据库空间
创建页大小为16k的临时数据库空间（空间名tempdbs01，chunk路径/data/gbase/tempdbs01chk001，偏移量为0，大小1024000KB）
```
touch /data/gbase/tempdbs01chk001 
chmod 660 /data/gbase/tempdbs01chk001
onspaces -c -d tempdbs01 -p /data/gbase/tempdbs01chk001 -o 0 -s 1024000 -k 16 -t
```
### 示例2-创建智能大对象空间
创建智能大对象空间（空间名sbspace01，chunk路径/data/gbase/sbspace01chk001，偏移量为0，大小1024000KB）
```
touch /data/gbase/sbspace01chk001 
chmod 660 /data/gbase/sbspace01chk001
onspaces -c -S sbspace01 -p /data/gbase/sbspace01 -o 0 -s 10240000
```
参考文档【[GBase 8s 管理员参考.pdf](https://www.dbboys.com/dl/gbase8s/docs/Administrator_Reference.pdf)】  
