### 创建系统用户
GBase 8s 默认支持使用系统用户连接数据库，要创建数据库用户，在操作系统创建系统用户即可。
如以下示例创建名为user1的数据库用户,【root】用户执行：
```
useradd user1
passwd user1
```

### 删除系统用户
【root】用户执行：
```
userdel user1
```
用户授权参考【[用户创建和删除（数据库用户）](用户创建和删除（数据库用户）.md)】