
### 创建NOLOG数据库  
```
create database mydb in datadbs01;
```
无日志记录数据库有最好的性能，但不支持事务，适用开发测试。
### 创建BUFFERED LOG数据库
```
create database mydb in datadbs01 with buffered log;
```
BUFFERED日志模式事务缓存在BUFFER中，BUFFER满了之后才写入磁盘，断电等极端故障可能存在丢失事务的可能，适用对数据完整性要求不高的一般业务系统或开发测试环境，允许故障发生后丢失少量事务。
### 创建UNBUFFERED LOG数据库
```
create database mydb in datadbs01 with log;
```
UNBUFFERED日志模式在事务提交后立即写入磁盘，具有最高的数据完整性，在极端故障场景下不会丢失数据，生产环境一般建议采用此日志模式。  
参考文档【[GBase 8s SQL 指南：语法.pdf](https://www.dbboys.com/dl/gbase8s/docs/Guide_to_SQL_Syntax.pdf) 】
