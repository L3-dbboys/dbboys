### 安装报错Could not initialize class sun.awt.X11GraphicsEnvironmen
错误信息
```
java.lang.NoclassDefFoundError:Could not initialize class sun.awt.X11GraphicsEnvironmen
```

错误原因
```
Xll转发未关闭
```

解决方案
```
关闭x11转发,options->session options->Remote/X11->Forward X11 packets去掉勾选
```
### 安装报错libnsl.so.1 cannot open shared object file
错误信息
```
libnsl.so.1: cannot open shared object file  #安装报错
```

错误原因
```
高版本系统安装数据库
```

解决方案
```
ln -s /usr/lib64/libnsl.so.2 /usr/lib64/libnsl.so.1
```

### 安装报错The version file is not exist
错误信息
```
安装时报错 The version file is not exist.
```

错误原因
```
缺少unzip
```

解决方案
```
#安装unzip
yum install unzip
```

### 安装报错You are logged on with a non-root user ID
错误信息
```
安装时报错 You are logged on with a non-root user ID.
```

错误原因
```
操作系统为最小化安装
```

解决方案
```
yum groupinstall “Development Tools”
```

