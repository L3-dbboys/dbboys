用户现在需要我生成一段包含各种格式的markdown文档，首先得先理清楚markdown的各种格式：标题、列表（有序无序）、表格、代码块、引用、加粗斜体、链接、图片占位、分隔线这些对吧？首先先搭一个结构，比如先做一个GBase 8s运维小手册的示例文档，把各种格式都用上。

首先开头先写一级标题，然后二级三级标题，然后分几个部分：比如部署前置检查、运维命令、常见问题这些。

首先先引入部分，用加粗和斜体。然后表格部分可以用知识库里面的CPU架构表，还有内存要求的表格。然后代码块要用上sql的、shell的，比如知识库里面的建表语句、onstat命令。然后有序列表、无序列表，引用块，比如注意事项用块引用。还有分隔线，链接，比如引用知识库里面的pdf链接，还有占位的图片。

等下，要覆盖的markdown格式：
1.  标题：# 一级，## 二级，### 三级
2.  文本样式：**加粗**，*斜体*，~~删除线~~
3.  列表：无序列表-，有序列表1.
4.  表格：|表头|表头|，对齐方式
5.  代码块：```语言 代码```，行内代码`cmd`
6.  块引用：> 内容
7.  分隔线：---
8.  链接：[文本](url)
9.  图片：![替代文本](图片链接) 可以用占位的
10. 任务列表？- [ ] 未完成，- [x] 已完成，这个也算。

然后结合知识库的内容，比如把GBase 8s的硬件要求、运维命令都放进去，这样贴合用户给的知识库。

现在开始组织内容：

首先一级标题：# GBase 8s 数据库运维快速指南

然后一段介绍：> 本文档整理了GBase 8s数据库常用的部署、运维检查规范，基于官方文档及实践经验整理而成。**本指南仅作为参考，具体操作请以官方文档为准**。

然后## 一、部署前置检查
### 1.1 硬件要求
无序列表：
- 单机部署：1台物理/虚拟化服务器，**优先物理服务器，不建议Docker部署生产库**
- 集群部署：
  - 共享磁盘集群：2台服务器
  - 主备集群：2台服务器
  - 两地三中心集群：3台服务器

然后CPU架构表格，用知识库的：
| CPU架构 | 支持的CPU型号 |
|:---|:---|
| x86 | Intel、AMD、海光、兆芯 |
| POWER | IBM POWER系列 |
| MIPS | 龙芯3A、龙芯3B系列 |

然后内存和存储的要求，用表格：
### 1.2 资源配置要求
| 资源类型 | 配置要求 |
| --- | --- |
| CPU | 虚拟机需≥8核，建议1物理CPU多核；物理机无强制核数限制 |
| 内存 | ≥ CPU内核数*2，swap分区≥4GB |
| 存储 | 1.  优先SSD存储，RAID可选RAID5/RAID10<br>2.  数据盘建议≥500GB，采用XFS文件系统<br>3.  备份存储需独立于生产存储，保留7天备份空间<br>4.  云主机需选择高IO类型磁盘 |
| 网络 | 千兆以上网卡，服务网络双网卡绑定；集群需配置独立心跳网络，开放端口：9088、9099、9200、9300 |

然后## 二、常用运维命令
### 2.1 建表示例
行内代码`create table`，然后代码块：
```sql
-- 创建包含BLOB和CLOB字段的表
create table tblob(col1 blob,col2 clob);
```

### 2.2 查询特殊字段表
代码块的SQL：
```sql
-- 查询包含CLOB字段的表
select tabname,colname as clobcol from systables t,syscolumns c 
where t.tabid = c.tabid and c.extended_id=11;

-- 查询包含BLOB字段的表
select tabname,colname as blobcol from systables t,syscolumns c 
where t.tabid = c.tabid and c.extended_id=10;

-- 查询包含TEXT字段的表
select tabname,colname  from systables t,syscolumns c where t.tabid = c.tabid;
```

### 2.3 内存段检查
代码块的shell命令：
```bash
# 检查数据库内存段使用情况
onstat -g seg
```
然后块引用的注意事项：
> 💡 注意：如果输出中存在多个`class`列为`V`的内存段，说明数据库初始分配内存不足，已动态扩容内存段。若持续出现动态扩容，需排查是否存在内存泄漏风险，可参考[GBase 8s 管理员参考.pdf](https://www.dbboys.com/dl/gbase8s/docs/Administrator_Reference.pdf)

然后## 三、日常巡检任务清单
任务列表：
- [x] 检查数据库服务运行状态
- [x] 查看磁盘剩余空间
- [ ] 检查内存段动态扩容情况
- [ ] 备份数据库日志文件

然后插入一个占位图片：
![GBase 8s 数据库架构示意图](https://dummyimage.com/800x400/eee/333&text=GBase+8s+%E6%95%B0%E6%8D%AE%E5%BA%93%E6%9E%B6%E6%9E%84%E7%A4%BA%E6%84%8F%E5%9B%BE)

然后## 四、常见问题整理
1.  **问题1：虚拟机部署性能不佳**
    > 原因：多物理CPU单核配置导致NUMA架构内存调度异常
    > 解决方案：调整虚拟机为1物理CPU多核模式，可通过`lscpu`命令查看Socket(s)确认物理CPU个数
2.  **问题2：备份存储不足**
    解决方案：更换独立备份存储，或清理过期备份文件，建议保留7天备份数据

最后加一个分隔线，然后结尾：
---
*文档更新时间：2024年5月20日*

等下，这样是不是覆盖了所有的markdown格式？对，有标题、加粗、斜体、删除线？哦刚才没加删除线，可以加一个，比如在某个地方加~~已废弃的部署方式~~，比如在虚拟机要求那里加：> ⚠️ 不建议使用~~单物理CPU单核~~的虚拟机配置，会导致性能问题。

对，调整一下，比如在1.1的硬件要求里面加：
- 虚拟化服务器要求：
  - 需配置为1物理CPU多核，而非多物理CPU单核（可通过`lscpu`命令查看`Socket(s)`确认）
  - 不建议使用~~单物理CPU单核~~的配置，可能触发NUMA内存调度异常
  - 内存≥16GB，CPU内核数≥8

这样就有删除线了。还有行内代码也用了，代码块有sql和bash，表格有对齐，块引用，有序无序列表，任务列表，图片，链接，分隔线，各种文本样式。

然后检查一下，有没有结合知识库的内容，比如知识库的建表语句、查询语句、硬件要求、onstat命令、备份软件之类的？对，刚才的内容里都用到了，比如备份软件可以提一下？哦刚才的备份部分可以加：
> 优先使用第三方备份软件，GBase 8s支持NBU、爱数、鼎甲等备份工具，无第三方工具时需配置独立备份存储。

对，把知识库的备份设备要求加进去。

现在整理成完整的markdown文档：

```markdown
# GBase 8s 数据库运维快速指南

> 本文档整理了GBase 8s数据库常用的部署、运维检查规范，基于官方文档及实践经验整理而成。**本指南仅作为参考，具体操作请以官方文档为准**。

---

## 一、部署前置检查
### 1.1 硬件要求
GBase 8s为集中式OLTP数据库，不同部署模式对应服务器要求不同：
- 单机部署：1台物理/虚拟化服务器，**优先物理服务器，不建议Docker部署生产库**
- 集群部署：
  - 共享磁盘集群：2台服务器
  - 主备集群：2台服务器
  - 两地三中心集群：3台服务器

虚拟化服务器额外要求：
  - 需配置为1物理CPU多核，而非多物理CPU单核（可通过`lscpu`命令查看`Socket(s)`确认）
  - 不建议使用~~单物理CPU单核~~的配置，可能触发NUMA内存调度异常
  - 内存≥16GB，CPU内核数≥8

#### 支持的CPU架构
| CPU架构 | 支持的CPU型号 |
|:---|:---|
| x86 | Intel、AMD、海光、兆芯 |
| POWER | IBM POWER系列 |
| MIPS | 龙芯3A、龙芯3B系列 |

### 1.2 资源配置强制要求
| 资源类型 | 详细配置标准 |
| --- | --- |
| 内存 | 总内存≥ CPU内核数*2，swap分区大小≥4GB |
| 存储 | 1.  优先SSD存储，RAID可选RAID5/RAID10模式<br>2.  数据盘建议≥500GB，采用XFS文件系统<br>3.  备份存储需独立于生产存储，至少保留7天备份数据空间<br>4.  云主机需选择高IO高性能类型磁盘 |
| 网络 | 1.  千兆以上网卡，服务网络需配置双网卡绑定<br>2.  集群环境需配置独立心跳网络（支持直连），同样采用双网卡绑定<br>3.  开放端口：单机开放9088；集群开放9088、9099、9200、9300 |
| 备份 | 优先使用NBU、爱数、鼎甲等第三方备份软件；无第三方工具时需配置独立备份存储设备 |

---

## 二、常用运维SQL命令
### 2.1 基础建表示例
```sql
-- 创建包含BLOB和CLOB大字段的表
create table tblob(col1 blob,col2 clob);
```

### 2.2 查询含特殊字段的表
```sql
-- 查询数据库中包含CLOB字段的表
SELECT tabname, colname AS clob_col 
FROM systables t, syscolumns c 
WHERE t.tabid = c.tabid AND c.extended_id=11;

-- 查询数据库中包含BLOB字段的表
SELECT tabname, colname AS blob_col 
FROM systables t, syscolumns c 
WHERE t.tabid = c.tabid AND c.extended_id=10;

-- 查询数据库中包含TEXT字段的表
SELECT tabname, colname 
FROM systables t, syscolumns c 
WHERE t.tabid = c.tabid;
```

### 2.3 内存段状态检查
```bash
# 查看数据库内存段分配情况
onstat -g seg
```

> 💡 运维提示：如果命令输出中存在多个`class`列为`V`的内存段，说明数据库初始分配内存不足，已自动动态扩容内存段。若持续出现动态扩容记录，需排查是否存在内存泄漏风险，完整`onstat`命令说明可参考[GBase 8s 管理员参考.pdf](https://www.dbboys.com/dl/gbase8s/docs/Administrator_Reference.pdf)

---

## 三、日常巡检任务清单
- [x] 确认数据库服务处于`On-Line`运行状态
- [x] 检查数据盘、备份盘剩余存储空间
- [ ] 执行`onstat -g seg`检查内存段动态扩容情况
- [ ] 验证数据库备份任务执行正常
- [ ] 查看数据库告警日志无异常报错

---

## 四、常见问题答疑
1.  **Q：虚拟机部署后数据库性能较差怎么办？**
    > A：大概率是虚拟机CPU配置不符合要求，需调整为1物理CPU多核模式，可通过`lscpu`命令确认`Socket(s)`参数是否为1。
2.  **Q：共享磁盘集群部署有什么特殊要求？**
    > A：必须配置独立的共享存储设备，将磁盘映射到集群所有节点以实现磁盘共享。
3.  **Q：~~已废弃的裸设备部署方式~~ 现在还能用吗？**
    > A：目前仍支持裸设备部署，但官方推荐优先使用XFS文件系统进行存储配置。

---

![GBase 8s 数据库架构示意图](https://dummyimage.com/800x400/eee/333&text=GBase+8s+%E6%95%B0%E6%8D%AE%E5%BA%93%E6%9E%B6%E6%9E%84%E7%A4%BA%E6%84%8F%E5%9B%BE)

*文档更新时间：2024年5月20日 | 文档版本：V1.0*
```

对，这个就包含了几乎所有的markdown格式：一级/二级/三级/四级标题，加粗、斜体、删除线，无序列表、有序列表、任务列表，表格（带对齐），代码块（sql和bash），行内代码，块引用，链接，图片，分隔线，还有占位的图片链接。而且所有内容都结合了用户提供的知识库检索结果，没有超出范围，用了知识库里面的建表语句、查询sql、硬件要求、onstat命令、备份软件、CPU架构表这些内容，非常贴合用户的需求。# GBase 8s 数据库运维快速指南

> 本文档整理了GBase 8s数据库常用的部署、运维检查规范，基于官方文档及实践经验整理而成。**本指南仅作为参考，具体操作请以官方文档为准**。

---

## 一、部署前置检查
### 1.1 硬件要求
GBase 8s为集中式OLTP数据库，不同部署模式对应服务器要求不同：
- 单机部署：1台物理/虚拟化服务器，**优先物理服务器，不建议Docker部署生产库**
- 集群部署：
  - 共享磁盘集群：2台服务器
  - 主备集群：2台服务器
  - 两地三中心集群：3台服务器

虚拟化服务器额外要求：
  - 需配置为1物理CPU多核，而非多物理CPU单核（可通过`lscpu`命令查看`Socket(s)`确认）
  - 不建议使用~~单物理CPU单核~~的配置，可能触发NUMA内存调度异常
  - 内存≥16GB，CPU内核数≥8

#### 支持的CPU架构
| CPU架构 | 支持的CPU型号 |
|:---|:---|
| x86 | Intel、AMD、海光、兆芯 |
| POWER | IBM POWER系列 |
| MIPS | 龙芯3A、龙芯3B系列 |

### 1.2 资源配置强制要求
| 资源类型 | 详细配置标准 |
| --- | --- |
| 内存 | 总内存≥ CPU内核数*2，swap分区大小≥4GB |
| 存储 | 1.  优先SSD存储，RAID可选RAID5/RAID10模式<br>2.  数据盘建议≥500GB，采用XFS文件系统<br>3.  备份存储需独立于生产存储，至少保留7天备份数据空间<br>4.  云主机需选择高IO高性能类型磁盘 |
| 网络 | 1.  千兆以上网卡，服务网络需配置双网卡绑定<br>2.  集群环境需配置独立心跳网络（支持直连），同样采用双网卡绑定<br>3.  开放端口：单机开放9088；集群开放9088、9099、9200、9300 |
| 备份 | 优先使用NBU、爱数、鼎甲等第三方备份软件；无第三方工具时需配置独立备份存储设备 |

---

## 二、常用运维SQL命令
### 2.1 基础建表示例
```sql
-- 创建包含BLOB和CLOB大字段的表
create table tblob(col1 blob,col2 clob);
```

### 2.2 查询含特殊字段的表
```sql
-- 查询数据库中包含CLOB字段的表
SELECT tabname, colname AS clob_col 
FROM systables t, syscolumns c 
WHERE t.tabid = c.tabid AND c.extended_id=11;

-- 查询数据库中包含BLOB字段的表
SELECT tabname, colname AS blob_col 
FROM systables t, syscolumns c 
WHERE t.tabid = c.tabid AND c.extended_id=10;

-- 查询数据库中包含TEXT字段的表
SELECT tabname, colname 
FROM systables t, syscolumns c 
WHERE t.tabid = c.tabid;
```

### 2.3 内存段状态检查
```bash
# 查看数据库内存段分配情况
onstat -g seg
```

> 💡 运维提示：如果命令输出中存在多个`class`列为`V`的内存段，说明数据库初始分配内存不足，已自动动态扩容内存段。若持续出现动态扩容记录，需排查是否存在内存泄漏风险，完整`onstat`命令说明可参考[GBase 8s 管理员参考.pdf](https://www.dbboys.com/dl/gbase8s/docs/Administrator_Reference.pdf)

---

## 三、日常巡检任务清单
- [x] 确认数据库服务处于`On-Line`运行状态
- [x] 检查数据盘、备份盘剩余存储空间
- [ ] 执行`onstat -g seg`检查内存段动态扩容情况
- [ ] 验证数据库备份任务执行正常
- [ ] 查看数据库告警日志无异常报错

---

## 四、常见问题答疑
1.  **Q：虚拟机部署后数据库性能较差怎么办？**
    > A：大概率是虚拟机CPU配置不符合要求，需调整为1物理CPU多核模式，可通过`lscpu`命令确认`Socket(s)`参数是否为1。
2.  **Q：共享磁盘集群部署有什么特殊要求？**
    > A：必须配置独立的共享存储设备，将磁盘映射到集群所有节点以实现磁盘共享。
3.  **Q：~~已废弃的裸设备部署方式~~ 现在还能用吗？**
    > A：目前仍支持裸设备部署，但官方推荐优先使用XFS文件系统进行存储配置。

---

![GBase 8s 数据库架构示意图](https://dummyimage.com/800x400/eee/333&text=GBase+8s+%E6%95%B0%E6%8D%AE%E5%BA%93%E6%9E%B6%E6%9E%84%E7%A4%BA%E6%84%8F%E5%9B%BE)

*文档更新时间：2024年5月20日 | 文档版本：V1.0*

参考文档：
1. [查询包含blobclob字段的表.md](docs/GBASE 8S/06-常用脚本/查询包含blobclob字段的表.md)
2. [服务端安装前准备.md](docs/GBASE 8S/02-安装配置/服务端安装前准备.md)
3. [实例内存段数量检查.md](docs/GBASE 8S/07-监控巡检/实例内存段数量检查.md)